#include "modules.h"
#include <stdbool.h>

bool ConnectToModule(ModuleId id)
{
    // load module state (from file or create new)
    // check power component
    // if no power return false
    // check network component
    // if no network return false
    
    //set current module to id
    //if fs.state == ONLINE
    // set Interface to fs.state == ONLINE ? CLI : MINCLI
    //if fs.state == 
    // return true
    return false;
}